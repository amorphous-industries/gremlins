stages:
  - name: github-discover-repo
    type: exec
    skip_if_exists: repo
    bind:
      artifact.repo: "file://session/repo.txt"
    options:
      cmds:
        - 'gh repo view --json nameWithOwner -q ".nameWithOwner" | tr -d "\n" > "{artifact_dir}/repo.txt"'