# Machine-specific bootstrap commands.
# Run in every worktree (main gremlin + parallel children) before stages begin.
# Prepended onto bootstrap.cmds (every-worktree setup), not launch_cmds.
#
# Add entries like:
#   - echo "PATH=$HOME/.cargo/bin:$PATH" >> "$GREMLINS_BOOTSTRAP_CWD/.env-extra" || true
