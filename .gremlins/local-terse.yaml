default_client: openrouter:deepseek-v4-pro
base_ref: current
prompt_dir: prompts

bootstrap:
  source:
    instructions:
      type: string
      optional: true
    plan:
      type: filepath
      optional: true
  launch_cmds:
    - |
      if [ -n "${plan+x}" ]; then
        if echo "$plan" | grep -qE '^([^/]+/[^#]+)?#[0-9]+$'; then
          gh issue view "$plan" --json body --jq .body > "{artifact_dir}/plan.md" 2>/dev/null
          if ! head -1 "{artifact_dir}/plan.md" | grep -q '^# ' 2>/dev/null; then
            title=$(gh issue view "$plan" --json title --jq .title 2>/dev/null)
            printf '# %s\n\n' "$title" | cat - "{artifact_dir}/plan.md" > "{artifact_dir}/plan.md.tmp"
            mv "{artifact_dir}/plan.md.tmp" "{artifact_dir}/plan.md"
          fi
          gh issue view "$plan" --json number --jq .number > "{artifact_dir}/plan-source-issue-number.txt" 2>/dev/null
        elif [ -f "$plan" ]; then
          cp "$plan" "{artifact_dir}/plan.md"
        else
          printf '%s' "$plan" > "{artifact_dir}/plan.md"
        fi
      fi
    - |
      if [ -n "${instructions+x}" ]; then printf '%s' "$instructions" > "{artifact_dir}/instructions.txt"; fi
  cli_out:
    plan?: file://session/plan.md
    plan-source-issue-number?: file://session/plan-source-issue-number.txt
    instructions?: file://session/instructions.txt
  cmds:
    - uv venv .venv
    - uv pip install -e ".[dev]"

prompts:
  code-style: gremlins:code_style.md
  terse: terse_style.md
  verify: [dev-env-howto.md, verify_fix.md, gremlins:bail_section_fix.md]
  dev-env: dev-env-howto.md

stage-definitions:
  normalize:
    type: exec
    options:
      cmds:
        - "ruff format ."
        - "ruff check --fix ."
        - "git add -A"
        - "git diff --cached --quiet || git commit -m 'normalize'"

stages:
  - { type: gremlins:plan,      prompt: [code-style, terse, gremlins:plan.md] }
  - { type: gremlins:implement,  prompt: [code-style, terse, dev-env, no_lint_in_implement.md, gremlins:implement_local.md] }
  - { type: normalize }
  - { name: verify-check, type: verify, options: { cmds: ["make check"] }, prompt: verify }

  - { type: gremlins:review-code,  prompt: [code-style, terse, dev-env, gremlins:review/detail.md] }
  - { name: address-code, type: agent, in: {text: review-code}, prompt: [code-style, terse, dev-env, gremlins:address.md, gremlins:bail_section.md] }

  - { type: normalize }
  - { name: verify-check-final, type: verify, options: { cmds: ["make check"] }, prompt: verify }
  - { name: verify-test, type: verify, options: { cmds: ["make test"] }, prompt: verify }
