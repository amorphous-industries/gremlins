default_client: xai:grok-4
base_ref: main
prompt_dir: prompts

land:
  in:
    PR_URL: pr-url
  options:
    cmds:
      - gh pr merge --squash --delete-branch "$PR_URL"

prompts:
  code-style: gremlins:code_style.md
  terse: terse_style.md
  dev-env: dev-env-howto.md

# Usage:
#   gremlins resume --graft gh-address <gremlin_id>
#
# Re-runs the address-reviews / format / wait-CI tail of gh-terse.yaml.
# Use when reviewer comments on an existing PR need another pass.

bootstrap:
  cmds:
    - uv venv .venv
    - uv pip install -e ".[dev]"

stages:
  - { name: github-address-pull-request-reviews, type: agent, in: {pr: pr}, prompt: [code-style, terse, dev-env, gremlins:github_address_pull_request_reviews.md, gremlins:bail_section.md] }
  - type: exec
    options:
      cmds:
        - "ruff format ."
        - "git add -A"
        - "git diff --cached --quiet || git commit -m 'normalize'"
  - { type: gremlins:github-wait-ci, prompt: [terse, dev-env, gremlins:ci_fix.md, gremlins:bail_section_fix.md] }
