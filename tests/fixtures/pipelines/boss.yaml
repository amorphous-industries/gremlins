default_client: 'xai:grok-4'

base_ref: current

github_integration: true

prompts:
  code-style: gremlins:code_style.md

stages:
  - name: chain
    type: loop
    body:
      - { name: handoff, type: gremlins:handoff }
      - { include: gh }

  - name: review-chain
    type: review-code
    prompt: [code-style, gremlins:review/chain.md]

  - name: address-chain
    type: agent
    prompt: [code-style, gremlins:address.md, gremlins:bail_section.md]
    interpolation: {text: artifact.review-chain}